type Message @model {
  username: String!
  avatar: URL
  body: String!
  likes: Int @default(value: 1952)
}
type Profile @model {
  username: String!
  avatar: URL
  body: String!
  likes: Int @default(value: 0)
}
